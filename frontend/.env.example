# Optional: full API base including /api, no trailing slash.
# Leave empty when the Vue dev server proxies /api → PHP (see vite.config.ts; run `php -S 127.0.0.1:8888` in backend/api).
# Production example (API on same host):
# VITE_API_BASE_URL=https://yourdomain.com/api
